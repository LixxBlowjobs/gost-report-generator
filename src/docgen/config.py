LLM_FALLBACK_CHAIN = [
    "google/gemini-2.0-flash-001",
    "google/gemma-3-27b-it:free",
    "nvidia/nemotron-3-super-120b-a12b:free",
]

OPENROUTER_BASE_URL = "https://openrouter.ai/api/v1"

# ── System prompt (общий для всех LLM-вызовов) ──
SYSTEM_PROMPT = """Ты — ассистент-исследователь, помогающий написать отчёт о НИР по ГОСТ 7.32-2017.

Правила:
- Язык: русский (кроме терминов и названий на английском)
- Стиль: научно-технический, академический. Избегай общих фраз и воды.
- Будь конкретен, опирайся на данные проекта.
- Формат: JSON по указанной схеме, строго без markdown-обёртки, без пояснений вне JSON.
- Ссылки на источники: [N] в тексте, где N — номер из списка литературы.
- НЕ используй: delve, landscape (метафора), realm, paradigm, leverage, robust, comprehensive, cutting-edge, seamless, pivotal, embark, testament to, meticulous, beacon, tapestry, game-changer, hit differently, utilize, vibrant, nestled, watershed, intricate, navigate (метафора), revolutionize.
- НЕ используй em-dash (—) в избытке. Один на 1000 слов максимум.
- НЕ используй конструкции «не X — а Y», «не просто X, а Y», «не только X, но и Y».
- НЕ хеджируй: убирай «возможно», «потенциально», «стоит отметить», «важно понимать».
- НЕ используй bold для случайных фраз.
- Каждый абзац — 2-4 предложения, связаны переходом."""

# ── Anaylzer prompt ──
ANALYZER_PROMPT = """Ты — архитектурный анализатор кода. Тебе дано дерево проекта, конфигурационные файлы и содержимое ключевых файлов.

Проанализируй проект и верни JSON (строго, без markdown-обёртки, без пояснений):
{
  "project_name": "название проекта",
  "purpose": "краткое описание назначения (1-2 предложения)",
  "tech_stack": ["Python", "FastAPI", ...],
  "architecture": "описание архитектуры (2-3 предложения)",
  "entry_point": "главный файл",
  "features": ["функция 1", "функция 2", ...],
  "confidence": "high|medium|low",
  "modules": [
    {
      "name": "путь к модулю",
      "purpose": "что делает",
      "loc": 123,
      "key_functions": ["func1", "func2"],
      "key_classes": ["Class1"],
      "depends_on": ["модуль2"]
    }
  ],
  "total_files": 45,
  "total_loc": 8200
}"""

# ── Plan prompt ──
PLAN_PROMPT = """Составь подробный план отчёта о НИР по проекту.

Данные анализа проекта:
{analysis}

Метаданные:
{metadata}

Требования к плану:
- 3 главы, каждая с 2-4 подразделами
- 4-6 тезисов введения
- 3-5 тезисов заключения
- 10-15 тем для источников литературы
- Глава 1: Анализ предметной области (обзор, требования, архитектурные решения)
- Глава 2: Проектирование системы (архитектура, модули, схема данных)
- Глава 3: Реализация и тестирование (технологии, реализация модулей, тесты)

Для каждого подраздела укажи: заголовок, 2-3 тезиса, флаги (needs_table, needs_figure, needs_code), темы ссылок.

Верни JSON строго без markdown:
{{
  "intro_thesis": ["тезис1 для введения", ...],
  "conclusion_thesis": ["тезис1 для заключения", ...],
  "reference_topics": ["тема 1", "тема 2", ...],
  "chapters": [
    {{
      "num": 1,
      "name": "АНАЛИЗ ПРЕДМЕТНОЙ ОБЛАСТИ",
      "subsections": [
        {{
          "num": "1.1",
          "heading": "Обзор существующих решений",
          "thesis": ["тезис1", "тезис2", "тезис3"],
          "needs_table": true,
          "needs_figure": true,
          "needs_code": false,
          "ref_topics": ["тема ссылки 1", "тема ссылки 2"]
        }}
      ]
    }}
  ]
}}"""

SECTION_PROMPTS = {
    "abstract": """Напиши реферат отчёта о НИР по ГОСТ 7.9.

Данные анализа проекта:
{analysis}

Метаданные:
{metadata}

Требования:
- Структура текста: объект → цель → методы → результаты → область применения
- Длина текста: около 850 символов, максимум 1 страница
- Объект исследования, цель работы, методы, результаты и новизна, область применения
- Ключевые слова: 5-15 слов в именительном падеже, через запятую
- Сведения об объёме: формат «Отчёт {n_pages} с., {n_figures} рис., {n_tables} табл., {n_sources} источн., {n_apps} прил.»

Верни JSON строго без markdown:
{{
  "volume": "Отчёт ... с., ... рис., ... табл., ... источн., ... прил.",
  "keywords": ["слово1", "слово2", ...],
  "text": "связный текст реферата (один абзац, ~850 символов)"
}}""",

    "intro": """Напиши введение отчёта о НИР.

Данные анализа проекта:
{analysis}

Метаданные:
{metadata}

План введения (тезисы):
{intro_thesis}

Требования:
- 5-8 абзацев, каждый 2-4 предложения, связаны переходом
- Структура: актуальность → состояние проблемы → цель работы → задачи (нумерованный список) → объект и предмет → методы
- 3-5 ссылок [N] на источники
- Завершить нумерованным списком задач (3-5 пунктов): «— проанализировать...», «— спроектировать...», «— реализовать...»

Верни JSON строго без markdown:
{{
  "paragraphs": [
    "абзац1 с [1] ссылкой",
    "абзац2 с [2,3] ссылками",
    "Задачи работы:",
    "— проанализировать ...;",
    "— спроектировать ...;",
    "— реализовать ...;",
    "— провести тестирование ...."
  ]
}}""",

    "conclusion": """Напиши заключение отчёта о НИР.

Данные анализа проекта:
{analysis}

Метаданные:
{metadata}

План заключения (тезисы):
{conclusion_thesis}

Требования:
- 4-6 абзацев, каждый 2-4 предложения
- Подведение итогов по каждой задаче из введения
- 2-3 ссылки [N] на источники
- Перспективы развития / направления дальнейших исследований

Верни JSON строго без markdown:
{{
  "paragraphs": ["абзац1 с [1]", "абзац2 ...", ...]
}}""",

    "references": """Сформируй список литературы по ГОСТ 7.1-2003.

Данные анализа проекта:
{analysis}

Темы из плана:
{reference_topics}

Требования:
- 10-15 реальных библиографических ссылок по темам
- Формат ГОСТ 7.1: «Автор И. О. Название / И. О. Автор. — М.: Издательство, год. — 200 с.» или для электронных: «Автор И. О. Название [Электронный ресурс]. — URL: ... (дата обращения: ДД.ММ.ГГГГ).»
- Источники: книги, статьи, стандарты, документация
- Включи минимум: ГОСТ 7.32-2017, ГОСТ 7.1-2003, документацию на используемые технологии

Верни JSON строго без markdown:
{{
  "references": [
    "1. Иванов И. И. Название. — М.: Изд-во, 2020. — 250 с.",
    "2. ..."
  ]
}}""",
}

# ── Chapter prompt template ──
CHAPTER_PROMPT = """Напиши главу {chapter_num} отчёта о НИР: «{chapter_name}».

Данные анализа проекта:
{analysis}

Метаданные:
{metadata}

План главы (подразделы):
{plan}

Доступные источники литературы (используй ссылки [N]):
{references}

Требования к каждому подразделу:
- 2-4 содержательных абзаца по тезисам плана
- Каждый абзац 2-4 предложения, связаны переходом
- Минимум 1 ссылка [N] на источник в подразделе
- Если в плане флаг needs_table: создай таблицу 3-5 строк с осмысленными данными
- Если needs_figure: добавь рисунок с описанием скриншота
- Если needs_code: добавь листинг реального кода из проекта (12-20 строк)
- Текст связан с конкретными данными проекта, никаких общих фраз

Верни JSON строго без markdown:
{{
  "sections": [
    {{
      "heading": "{first_subsection_heading}",
      "paragraphs": ["абзац1 с [1]", "абзац2 ...", "абзац3 ...", "абзац4 ..."],
      "tables": [
        {{
          "caption": "Название таблицы",
          "headers": ["колонка1", "колонка2", "колонка3"],
          "rows": [["значение", "значение", "значение"], ...]
        }}
      ],
      "figures": [
        {{
          "comment": "описание содержимого рисунка/скриншота",
          "caption": "Название рисунка"
        }}
      ],
      "listings": [
        {{
          "caption": "Листинг N — Описание кода",
          "code": "def real_function():\\n    # реальный код из проекта\\n    pass"
        }}
      ]
    }}
  ]
}}

ВАЖНО: каждый подраздел из плана должен присутствовать в sections. Не возвращай пустые sections."""
